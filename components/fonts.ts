import { Bebas_Neue, Inter } from 'next/font/google'

export const titleFont = Bebas_Neue({ subsets: ['latin'], weight: '400', variable: '--font-title' })
export const bodyFont  = Inter({ subsets: ['latin'], variable: '--font-body' })
